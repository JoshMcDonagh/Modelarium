/**
 * Provides policies for ordering local agent execution on each tick.
 *
 * <p>Modelarium includes deterministic in-order and seeded random-order schedulers and supports custom scheduler
 * implementations.
 */
@modelarium.api.PublicApi
package modelarium.scheduler;
