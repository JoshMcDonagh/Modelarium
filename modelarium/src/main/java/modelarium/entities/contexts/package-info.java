/**
 * Defines the simulation views supplied to attribute behaviour.
 *
 * <p>Contexts provide the current entity, attribute, clock, configuration, seeded random generator, environment,
 * and the agents visible under the model's synchronisation mode.
 */
@modelarium.api.PublicApi
package modelarium.entities.contexts;
