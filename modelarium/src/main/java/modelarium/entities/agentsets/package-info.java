/**
 * Provides mutable and read-only collections of agents.
 *
 * <p>Agent sets are used by generators to partition a population between worker threads and by contexts to expose
 * the agents visible to running behaviour.
 */
@modelarium.api.PublicApi
package modelarium.entities.agentsets;
