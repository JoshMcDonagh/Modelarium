/**
 * Provides utility classes used by Modelarium's built-in attribute implementations.
 *
 * <p>
 * This package contains helper functionality that supports lookup, validation,
 * and other internal operations required by built-in properties, events, actions,
 * and value references. These utilities help keep the built-in implementations
 * concise and consistent.
 * </p>
 *
 * <p>
 * The classes in this package are primarily intended for internal framework use.
 * They may also be useful to advanced users implementing custom built-in-style
 * components that need to resolve properties or attribute sets in the same manner
 * as the standard built-in classes.
 * </p>
 */
package modelarium.attributes.builtins.util;