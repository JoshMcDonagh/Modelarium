/**
 * Implements Modelarium's worker and co-ordinator runtime.
 *
 * <p>This package is an internal implementation detail. Applications should configure concurrency through
 * {@link modelarium.Config} and interact with the running model through public contexts.
 */
@modelarium.internal.Internal
package modelarium.multithreading;
