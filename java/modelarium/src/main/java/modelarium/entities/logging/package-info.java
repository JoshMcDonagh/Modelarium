/**
 * Provides logs which associate an entity and attribute set with recorded attribute series.
 *
 * <p>Logs are normally created and managed by entities; applications generally consume them through the read-only
 * results API after a run.
 */
@modelarium.api.PublicApi
package modelarium.entities.logging;
