/**
 * Provides read-only access to simulation time.
 *
 * <p>Attribute logic normally obtains the run's clock from its simulation context rather than constructing a clock
 * directly.
 */
@modelarium.api.PublicApi
package modelarium.clock;
